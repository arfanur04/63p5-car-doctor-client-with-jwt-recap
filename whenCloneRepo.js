/* 
   check if needed then update
   1. change git remote set-url origin
   2. .env.local
   3. apiURL (like localhost)
   4. delete firebase init all files
   5. terminal: firebase init
   6. terminal: npm run build
   7. terminal: firebase deploy
 */
